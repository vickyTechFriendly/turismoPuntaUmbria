/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.gibraleon.com'],
        domains: ['192.168.1.13'],
      },
      // ... cualquier otra configuración existente
}

module.exports = nextConfig
